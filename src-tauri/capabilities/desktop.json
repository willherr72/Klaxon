{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions — autostart plugin is not compiled into the mobile builds, so its permissions can't be referenced there.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main", "alert-*"],
  "permissions": [
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
  ]
}
